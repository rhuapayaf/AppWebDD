<%= form_for(@neck) do |f| %>
  <% if @neck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@neck.errors.count, "error") %> prohibited this neck from being saved:</h2>

      <ul>
      <% @neck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
