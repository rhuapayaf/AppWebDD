<%= form_for(@sleefe) do |f| %>
  <% if @sleefe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sleefe.errors.count, "error") %> prohibited this sleefe from being saved:</h2>

      <ul>
      <% @sleefe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
